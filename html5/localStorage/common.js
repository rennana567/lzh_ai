const addItems = document.querySelector('.add-items'); //form
const itemsList = document.querySelector('.plates'); // 列表

function addItems(e){
    e.preventDefault(); // 阻止表单提交
    
}

addItems.addEventListenter('submit',addItems);